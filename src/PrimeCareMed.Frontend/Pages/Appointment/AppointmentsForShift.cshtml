@page "/shift/appointments/{id:Guid}"
@model PrimeCareMed.Frontend.Pages.Appointment.AppointmentsForShiftModel
@{
    ViewData["Title"] = "Appointments for shift";
}
<h1>@ViewData["Title"]</h1>
<hr />

<div class="form-actions no-color col">
    <form asp-action="ViewAllPatients" method="get">
        <div class="row">
            <div class="col-4 col-sm-7">
                @* <div class="input-group mb-3">*@
                @* <input type="search" class="form-control" placeholder="Search..." name="keyword" value="@keyword">
                <select class="form-select" aria-label="Example select with button addon" name="categoryFilter">
                <option value="">All categories</option>
                @foreach (var category in Enum.GetNames(typeof(DishCategory)))
                {
                <option value="@category" selected="@(category == categoryFilter)">@category</option>
                }
                </select>
                <button type="submit" class="btn btn-primary"><i class="bi bi-search"></i></button>
                </div>*@
            </div>
            @*<div class="col-1">
            <a asp-action="ViewAllDishes" class="btn btn-secondary clear-btn">Clear</a>
            </div>*@
        </div>
    </form>
</div>

<table class="table table-striped table-bordered user-table">
    <thead>
        <tr>
            @foreach(var property in Model.AppointmentModelProperties)
            {
                 <th>@property</th>
            } 
        </tr>
    </thead>
    <tbody>
        @if (Model.Appointments.Count() > 0)
        {
            @foreach (var appointment in Model.Appointments)
            {
                <tr class="align-middle table-info">
                    <td class="d-none d-lg-table-cell">@appointment.PatientMbo</td>
                    <td class="d-none d-lg-table-cell">@appointment.PatientFirstName</td>
                    <td class="d-none d-lg-table-cell">@appointment.PatientLastName</td>
                    <td class="d-none d-lg-table-cell">@appointment.AppointmentDate.ToString("dd.MM.yyyy HH:mm:ss")</td>
                    <td class="d-none d-lg-table-cell">@appointment.Cause</td>
                    <td>
                        <a class="btn btn-outline-primary" asp-page="ViewAppointmentDetails" asp-route-id="@appointment.Id"><i class="bi bi-info-lg"></i></a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>

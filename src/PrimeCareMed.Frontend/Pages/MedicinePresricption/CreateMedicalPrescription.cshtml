@page 
@model PrimeCareMed.Frontend.Pages.MedicinePresricption.CreateMedicalPrescriptionModel
@{
    ViewData["Title"] = "Add office";
}
<div class="container text-center d-flex align-items-center justify-content-center">
    <div>
        <div>
            <h1>@ViewData["Title"]</h1>
        </div>
        <div>
            <form id="createOfficeForm" method="post">
                <hr />
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                @*<div class="form-floating">
                    <input asp-for="NewOffice.Address" class="form-control" aria-required="true" />
                    <label asp-for="NewOffice.Address"></label>
                    <span asp-validation-for="NewOffice.Address" class="text-danger"></span>
                </div>
                <div class="form-floating">
                    <input asp-for="NewOffice.City" class="form-control" aria-required="true" />
                    <label asp-for="NewOffice.City"></label>
                    <span asp-validation-for="NewOffice.City" class="text-danger"></span>
                </div>*@
                <button id="createOfficeSubmit" type="submit" class="w-100 btn btn-lg btn-primary">Add office</button>
            </form>
            @if (ViewData["Message"] != null)
            {
                <script type="text/javascript">
                    window.onload = function () {
                        alert("@ViewData["Message"]");
                    };
                </script>
            }
        </div>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

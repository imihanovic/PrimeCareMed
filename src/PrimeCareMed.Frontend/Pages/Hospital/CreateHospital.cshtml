@page
@model PrimeCareMed.Frontend.Pages.Hospital.CreateHospitalModel
@{
    ViewData["Title"] = "New hospital";
}
<div class="container text-center d-flex align-items-center justify-content-center">
    <div>
        <div>
            <h1>@ViewData["Title"]</h1>
        </div>
        <div>
            <form id="createOfficeForm" method="post">
                <hr />
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-floating">
                    <input asp-for="NewHospital.Name" class="form-control" aria-required="true" />
                    <label asp-for="NewHospital.Name"></label>
                    <span asp-validation-for="NewHospital.Name" class="text-danger"></span>
                </div>
                <div class="form-floating">
                    <input asp-for="NewHospital.Address" class="form-control" aria-required="true" />
                    <label asp-for="NewHospital.Address"></label>
                    <span asp-validation-for="NewHospital.Address" class="text-danger"></span>
                </div>
                <div class="form-floating">
                    <input asp-for="NewHospital.City" class="form-control" aria-required="true" />
                    <label asp-for="NewHospital.City"></label>
                    <span asp-validation-for="NewHospital.City" class="text-danger"></span>
                </div>
                <button id="createOfficeSubmit" type="submit" class="w-100 btn btn-lg text-white" style="background-color: #006622">Confirm</button>
            </form>
            @if (ViewData["Message"] != null)
            {
                <script type="text/javascript">
                    window.onload = function () {
                        alert("@ViewData["Message"]");
                    };
                </script>
            }
        </div>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
